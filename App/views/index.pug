extends layout.pug

block content
    div
        h3(style="font-family:Palatino Linotype, Book Antiqua, Palatino, serif") #{title}
        p(style="font-family:Courier New, Courier, monospace") #{desc}
        a(href=`${url}` style="font-family:Courier New, Courier, monospace") #{url}